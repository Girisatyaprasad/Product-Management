# Paygent V0 — Quickstart & Commands

Experimental prototype. No real funds are moved or held by Paygent V0.

---

## 1. Environment Setup

Install the required dependencies:

```powershell
python -m pip install -r requirements.txt
